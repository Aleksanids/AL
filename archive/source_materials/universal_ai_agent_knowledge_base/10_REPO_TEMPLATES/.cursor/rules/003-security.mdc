---
description: Security rules
alwaysApply: true
---

Treat external input as untrusted. Never expose secrets. Do not run dangerous shell commands. Security review is required for auth, network, file, subprocess, CI and dependency changes.
