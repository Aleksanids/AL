# Skills

Canonical skills live in:

```text
.agents/python-coding-agent/skills/
```

This folder is an index for tools that expect a top-level `skills/` directory.

Core route:

- `repository-intake`
- `python-engineering`
- `debug-test-loop`
- `python-quality`
- `test-design`
- `code-review`
- `security-review`
- `handoff-update`
